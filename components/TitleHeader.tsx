import { Center, Icon, Box, Button } from "@chakra-ui/react";
import React from "react";
import { HeaderLogo, Vercel } from "../public/assets/icons";
const TitleHeader = () => {
  return (
    <Center h={14}>
      <Box></Box>
    </Center>
  );
};

export default TitleHeader;
